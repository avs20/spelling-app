# Turso Database Configuration (for Modal deployment)
# Get these values from: https://turso.tech/
TURSO_DATABASE_URL=libsql://your-database-name.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token-here

# Local development uses SQLite by default (no env vars needed)
# When these variables are set, the app will use Turso instead
